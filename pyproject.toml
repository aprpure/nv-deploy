[project]
name = "nvinda"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "curl-cffi>=0.16.0",
    "faker>=40.36.0",
    "hcaptcha-challenger>=0.19.0",
    "httpx[socks]>=0.28.1",
    "pydantic>=2.13.4",
    "pyyaml>=6.0.3",
]
